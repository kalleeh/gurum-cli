project:
  name: str()
  source:
    provider: str()
    repo: str()
  type: str()
environments: list(include('environment'))
services: list(include('service'), required=False)
---
environment:
  name: str()
  config: map()
  env_vars: list()

service:
  name: str()
  type: str()
  config: list()
